{
    "name": "@luminnus/core",
    "version": "0.1.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "lint": "echo 'No lint configured'",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@luminnus/shared": "workspace:*"
    },
    "devDependencies": {
        "rimraf": "^5.0.5",
        "typescript": "^5.3.0"
    }
}